# 🤖 Полная архитектура бота 2248: Обучение на ошибках

## 📋 Обзор системы

Бот для игры 2248 представляет собой комплексную систему с тремя основными слоями:

1. **Настройка окружения** — ADB, координаты, состояние игры
2. **Конфигурация и данные** — JSON файлы, обученные модели, история ошибок  
3. **Архитектура кода** — связанные компоненты с централизованным управлением

## 🏗️ Структура файлов

```
2248_deepseek/
├── main.py                 # Точка входа, запуск меню
├── bot.py                  # Главный фасад, координатор компонентов
├── ui.py                   # Консольное меню и пользовательский интерфейс
├── game_logic.py           # Логика игры + подключение эвристики
├── heuristics_2248.py      # Эвристическая оценка ходов
├── game_runner.py          # Основной игровой цикл
├── config_manager.py       # Центральное управление настройками
├── screen_processor.py     # Работа с экраном/ADB
├── input_controller.py     # Выполнение свайпов
├── ad_detector_2248.py     # Детекция и работа с рекламой
├── end_game_handler.py     # Детекция окончания игры
├── color_trainer.py        # Обучение распознаванию цветов
├── calibrator.py           # Калибровка сетки клеток
├── constants.py            # Константы и пути
├── board_printer.py        # Визуализация доски
├── config.json             # Основная конфигурация
├── problem_cells.json      # Проблемные клетки для дообучения
├── bad_moves.json          # Плохие ходы для избегания
├── cells/                  # Скриншоты клеток
├── moves/                  # Скриншоты ходов
├── end_screens/            # Шаблоны окончания игры
└── requirements.txt        # Зависимости
```

## 🔧 Слой 1: Настройка окружения

### ADB и подключение
- Рабочий ADB и одно подключённое устройство/эмулятор
- Проверка через пункт 1 меню "Проверить ADB"
- Правильные `EVENT_DEV`, `X_MIN/X_MAX/Y_MIN/Y_MAX` в `constants.py`

### Игровое состояние
- Игра 2248 открыта на игровом поле (не в меню)
- Бот распознаёт только пиксели, не различает меню/поле явно

## ⚙️ Слой 2: Конфигурация и данные

### config.json должен содержать:
- `"calibrated": true` — после обучения цветов
- `"grid"` — координаты центров 5×4 клеток
- `"colors"` — словарь `"число" | "adv" → [ [r,g,b], ... ]`

### problem_cells.json и bad_moves.json:
- Автоматически накапливаются через `GameLogic.remember_problem_cell`
- Хранятся через `ConfigManager.bad_moves` 
- Помогают отслеживать и избегать ошибок

### Рабочие директории:
- `cells/` — скриншоты клеток `cell_r_c.png`
- `moves/` — скриншоты ходов `move_N.png`, `after_move_N.png`
- `problem_cells/` — копии проблемных клеток (если включено)
- `end_screens/` — win/lose шаблоны и `end_button_template.png`

## 🧩 Слой 3: Архитектура кода

### main.py
- Создаёт `Auto2248Bot()` и вызывает `bot.show_menu()`

### bot.py (Auto2248Bot)
- Поднимает все компоненты: `ConfigManager`, `ScreenProcessor`, `InputController`, `GameLogic`, `ColorTrainer`, `EndGameHandler`, `EndGameAdDetector2248`, `GameRunner`, `UI`
- Связывает всё между собой (`set_ad_detector`, передаёт объекты в `GameRunner`, `UI`)
- Наружу даёт методы: `show_menu`, `run_auto_game`, `check_adb`, `take_screenshot`, `show_problem_cells`

### ui.py
- Отвечает за меню и пользовательский ввод
- Пункт 4: обучение цветов через `ColorTrainer.learn_colors_simple()`
- Пункт 5: распознать и отрисовать доску (`take_screenshot` → `crop_cells_from_screen` → `GameLogic.recognize_board_with_confidence` → `print_board`)
- Пункт 6: `bot.run_auto_game()` → основной игровой цикл в `GameRunner`
- Пункт 8: вызывает `bot.show_problem_cells()` (вывод `ConfigManager.problem_cells`)

### config_manager.py
- Единая точка для: `config` (grid, colors, thresholds), `problem_cells`, `bad_moves`

### screen_processor.py
- Всё по экрану/ADB: скрины, кропы, доставание цвета клетки

### game_logic.py
- Держит `board` 5×4
- Распознаёт доску (`recognize_board_with_confidence`)
- Собирает проблемные клетки (`remember_problem_cell` → `config_manager.problem_cells`)
- Ищет цепочки (`find_all_chains`, `_filter_chains`, `is_valid_chain`)
- Оценивает цепочки через эвристику (`evaluate_chain_smart` → `Heuristics2248`)
- Выбирает лучший ход (`find_best_chain_smart`), учитывая `bad_moves`
- Симулирует последствия (`simulate_board_after_move`)
- Запоминает плохие ходы (`remember_bad_move` → `bad_moves.json`)

### heuristics_2248.py
- Подключён в `GameLogic`: `self.heuristics = Heuristics2248(self)`
- `evaluate_chain_smart` делегирует в `self.heuristics.evaluate_chain`
- Тогда весь выбор `best_chain`/`best_pair` использует новую эвристику

### game_runner.py
- Основной цикл игры:
  - Скрин → кроп
  - `GameLogic.recognize_board_with_confidence`
  - `get_board_hash`, работа с `last_move_hash` / `current_move_attempts`
  - `find_best_chain_smart` → `evaluate_chain_smart`/`Heuristics2248`
  - `InputController.perform_chain_swipe_mt`
  - `EndGameHandler.check_and_restart` + `EndGameAdDetector2248` при отсутствии ходов
  - Опционально `print_board` при `show_board_each_move`

## ✅ Минимальный чек-лист "чтобы прекрасно работал"

### 1. Калибровка и обучение
- Пункт 1: ADB ок
- Пункт 2/3/12: сетка (`grid`) выставлена (или `use_preset=True` и координаты совпадают)
- Пункт 4: `ColorTrainer.learn_colors_simple()` пройден, `config["colors"]` заполнен, `calibrated=True`

### 2. Эвристика реально используется
- В `game_logic.py` импорт `Heuristics2248` и поле `self.heuristics`
- `evaluate_chain_smart` делегирует в `self.heuristics.evaluate_chain`

### 3. Runner не ломает старую логику
- `GameRunner.run_auto_game` максимально близок к старому `GameLogic.run_auto_game`
- После успешного свайпа обновляется доска и `last_move_hash` через «after_move» скрин

### 4. Отладка проблем и хренового распознавания
- `remember_problem_cell` пишет в `ConfigManager.problem_cells` и копирует картинки в `problem_cells/` (если включено)
- Пункт 8 меню вызывает `bot.show_problem_cells()`, который красиво печатает список проблемных клеток

### 5. Работа с рекламой/концом игры
- `EndGameHandler` корректно распознаёт win/lose по шаблонам в `end_screens/`
- `EndGameAdDetector2248` загружает `end_button_template.png` и используется при отсутствии ходов

## 🚀 Результат

Если все пункты выполнены:
- Бот всегда начинает ход с актуального скрина
- Достоверно распознаёт сетку и числа (с учётом цветового обучения)
- Выбирает ходы через одну точку — `Heuristics2248.evaluate_chain` → `find_best_chain_smart`
- Запоминает, где обосрался (problem_cells + bad_moves)
- Корректно реагирует на конец раунда и рекламу

Дальнейшая настройка возможна через веса в `heuristics_2248.py` и `heuristics_weights.json`.